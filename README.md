# -Network-security-scanner
NETRECON is a Python-based network security scanner for authorized environments. It performs TCP port checks, identifies common services, stores results in SQLite, and generates text and CSV reports.
